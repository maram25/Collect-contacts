<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_home">

    <fragment
        android:id="@+id/navigation_home"
        android:name="com.collect.contacts.UI.Home.HomeFragment"
        android:label="title_home"
        tools:layout="@layout/home_fragment" />

    <fragment
        android:id="@+id/navigation_favorite"
        android:name="com.collect.contacts.UI.ContactList.ContactListFragment"
        android:label="ContactList"
        tools:layout="@layout/contact_list_fragment" />

</navigation>